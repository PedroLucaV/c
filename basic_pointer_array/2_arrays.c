#include <stdio.h>

int main(){
    int arr[5] = {1, 2, 3, 4, 5};

    for(int i=0; i<5; i++){
        printf("index %d = %d", i, arr[i]); //para definir varias variaveis, define o tipo dela dentro do texto, depois informa a variavel na ordemss
    }

    return 0;
}