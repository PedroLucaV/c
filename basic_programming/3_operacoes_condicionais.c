#include <stdio.h>

int main(){
    int num1 = 10;
    int num2 = 9;

    if (num1 + num2 > 18 && num1 + num2 < 60){
        printf("Pode passar!");
    }
    else if (num1 + num2 >= 60)
    {
        printf("Não pode passar!");
    }
    else{
        printf("Não pode passar!");
    }
    return 0;
}